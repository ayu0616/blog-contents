# AGENTS.md

## 1. エージェントの役割 (Role)

エージェントがこのリポジトリで担当する主なタスクを定義します。

* **記事の執筆・推敲**: 新規Markdownファイルの作成、構成案の作成、文章のブラッシュアップ、誤字脱字の修正。
* **メタデータの管理**: Frontmatter（`published`, `tags`, `modified` など）の適切な維持管理。
* **アセットの最適化と整理**: 画像の追加、変換（`.webp`への自動変換）、未使用画像のクリーンアップ。

## 2. 記事の執筆ルールとフォーマット (Writing Rules & Formatting)

ブログの一貫性を保つためのトーン＆マナーや固有の記法を指示します。

* **トーン＆マナー**: 基本は「です・ます」調を採用し、読者に語りかけるような親しみやすい文体にする。
* **Frontmatter**:
  記事の先頭には必ず以下のYAML Frontmatterを含めること。新規作成時は `template/template.md` を参照するか、`new_page.py` を使用する。

  ```yaml
  ---
  published: false
  publishedAt: 
  tags: [タグ1, タグ2]
  created: YYYY-MM-DD HH:MM:SS+09:00
  modified: YYYY-MM-DD HH:MM:SS+09:00
  ---
  ```

* **目次の自動生成**:
  最初の見出し（`# タイトル`）の直後に以下のコードブロックを配置する。

  ````markdown
  ```table-of-contents
  minLevel: 2
  ```
  ````

* **Callout（補足）記法**:
  ヒントや警告などの補足情報には、プロジェクト固有のCallout記法を使用する。

  ````markdown
  ```callout
  ----
  icon: 💡
  title: タイトル
  ----
  ここに内容を記載
  ```
  ````

## 3. ディレクトリ構成とファイル管理 (File Management)

エージェントがファイルをどこに配置し、どう参照すべきかを指定します。

* **記事ファイル**: ルートディレクトリに `[slug].md` の形式で配置する。
* **画像ファイル**: すべて `assets/` ディレクトリに配置する。
* **画像パスの指定**: Markdownからは相対パスではなくファイル名のみ、あるいは `./assets/ファイル名` で参照する（例: `![altテキスト](image.webp)` または `![altテキスト](./assets/image.webp)` ）。
* **Lint規則**: `.markdownlint.jsonc` に従う（リストのインデントは4スペース、行長制限なし、インラインHTML許容など）。

## 4. 組み込みツールの活用 (Scripts & Tooling)

リポジトリ内に用意されているPythonスクリプト群（ツールの仕様）をエージェントに教え、適切なタイミングで実行させます。

* **新規ページの作成**: `python new_page.py [slug]`
    * 指定したslugでテンプレートが展開される。
* **画像のWebP変換**: `python to_webp.py .`
    * `assets/` 内のjpg, pngなどを自動でwebpに変換し、Markdown内の画像参照パスも書き換える。画像を追加した際は必ず実行する。
* **未使用アセットの削除**: `python clean_assets.py .`
    * 記事内で参照されていない `assets/` 内の画像を削除する。記事を大幅に修正・削除した後に実行する。
* **ページ情報の更新と検証**: `python update_page_info.py`
    * Callout記法のバリデーションと `page-info.json` の更新を行う。コミット前に検証として実行する。
* **GCSへの同期（公開時）**: `python sync-gcs.py`
    * 現在使用していないため、決して実行しないでください。

## 5. ワークフロー (Workflow Example)

エージェントに期待する具体的な作業手順を示します。

**【新規記事作成ワークフロー】**

1. ユーザーからテーマを与えられたら、`new_page.py` でファイルを作成。
2. 目次や見出し構成を生成し、本文を執筆。
3. 必要な画像を `assets/` に追加し、`to_webp.py` を実行。
4. 本文中の画像参照が正しいか確認。
5. 推敲を行い、トーン＆マナーやMarkdownlintの制約をクリアさせる。
6. `update_page_info.py` を実行してエラーが出ないかチェックする。

---
「あなたは技術・旅行・日常に関するブログの優秀なアシスタント・エディターです。既存の記事（`sannin-18kippu.md` や `python_match-case.md` など）の文体や雰囲気を学習し、ユーモアを交えつつも整理されたわかりやすい記事を執筆してください。ファイルを操作する際は、必ず本ドキュメントに記載されたスクリプトを活用し、プロジェクトの規約を守ってください。」
